#!/bin/bash

# get current branch
CURRENT_BRANCH=$(git rev-parse --symbolic-full-name --abbrev-ref HEAD)

if [ $1 ]; then
  git add --all &&
  git checkout $1 &&
  rebase-no-stash &&
  push-force &&
  git checkout $CURRENT_BRANCH
else
  rebase &&
  push-force
fi
