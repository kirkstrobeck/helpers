#!/bin/bash

# usage
# branch (string || branch)

# example
# branch fix-things

git fetch --all &&
git add --all &&
git stash &&
if [ $(git branch --all | grep $1) ]; then
  git checkout $1 &&
  deps-install
else
  master &&
  deps-install &&
  git checkout -b $1
fi &&
git stash pop
